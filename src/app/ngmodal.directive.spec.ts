import { NgmodalDirective } from './ngmodal.directive';

describe('NgmodalDirective', () => {
  it('should create an instance', () => {
    const directive = new NgmodalDirective();
    expect(directive).toBeTruthy();
  });
});
