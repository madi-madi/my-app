import { ForsanDirective } from './forsan.directive';

describe('ForsanDirective', () => {
  it('should create an instance', () => {
    const directive = new ForsanDirective();
    expect(directive).toBeTruthy();
  });
});
