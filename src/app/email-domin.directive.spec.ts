import { EmailDominDirective } from './email-domin.directive';

describe('EmailDominDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailDominDirective();
    expect(directive).toBeTruthy();
  });
});
