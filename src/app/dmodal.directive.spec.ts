import { DmodalDirective } from './dmodal.directive';

describe('DmodalDirective', () => {
  it('should create an instance', () => {
    const directive = new DmodalDirective();
    expect(directive).toBeTruthy();
  });
});
